# ---- private, never publish ----
private/
ASSIGNMENT_LOCK.json.bak
*.local.json

# ---- benchmark images ----
# The 600 benchmark images ARE published here, on the repository owner's explicit
# decision. Per-source provenance and licence text are recorded in the frozen manifests
# and in private/image_manifest.json.

# ---- returned annotations / test artefacts ----
returned/
*.annotations.json
visexmem_annotations_*.json
visexmem_backup_*.json

# ---- junk ----
.DS_Store
__pycache__/
*.pyc

# ---- researcher-only previews, never publish ----
preview_*.html
